// Generated by CoffeeScript 1.9.0
(function() {
  var getCinemas, getMovies, renderCinemas, renderMovies;

  $(function() {
    console.log('Wat');
    console.log('requesting movies');
    return getMovies();
  });

  getMovies = function() {
    return $.ajax({
      url: '/movies.json',
      dataType: 'json',
      error: function(jqXHR, textStatus, errorThrown) {
        return console.log("Error " + textStatus + ", " + errorThrown);
      },
      success: renderMovies
    });
  };

  getCinemas = function() {
    return $.ajax({
      url: '/cinemas.json',
      dataType: 'json',
      error: function(jqXHR, textStatus, errorThrown) {
        return console.log("Error " + textStatus + ", " + errorThrown);
      },
      success: renderCinemas
    });
  };

  renderMovies = function(movies) {
    return $('body').append("<h1>Wheeeeeeeeee</h1>");
  };

  renderCinemas = function(cinemas) {
    console.log("Found cinemas");
    return console.log(cinemas);
  };

}).call(this);
